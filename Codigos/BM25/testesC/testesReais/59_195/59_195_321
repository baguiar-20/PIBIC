#include <stdio.h>
#include <stdlib.h>

int main(){
int p,q,i;
int n;

scanf("%d",&p);
scanf("%d",&q);
if (p>q){
	for(i=1;i<=p;i++){
		if((p%i==0)&&(q%i==0))
		n=i;
		}
	}
else if (q>=p){
	for(i=1;i<=q;i++){
		if((p%i==0)&&(q%i==0))
		n=i;
	}
}
printf("%d",n);
return 0;
}
