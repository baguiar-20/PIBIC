#include <stdio.h>
#include <string.h>
int main () {
	char nome [100];
	int i, tam;

	printf ("Nome: ");
	fgets (nome, 100, stdin);
	
	
	
	nome[strlen(nome)-1] = '\0'; 
	tam = strlen (nome);
	printf ("Inverso: ");	
	for (i=tam-1 ; i >= 0 ; i--){
		printf ("%c", nome [i]);		
	}

	printf ("\n");
	
}
