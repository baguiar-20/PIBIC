#include <stdio.h>
#include <string.h>

int main() {
	char nome[100];
	int i;
	
	printf("Nome: ");
	fgets(nome, 100, stdin);
	
	printf("Inverso: ");
	for (i=strlen(nome)-2; i!=-1; i--) 
		printf("%c",nome[i]);

	printf("\n");
	return 0;	
}
