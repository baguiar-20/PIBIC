#include<stdio.h>
#include<stdlib.h>
#define N 30
main(void)
{
	int A[N];
	int i,j;
	for(i=0;i<N;i++)
	{
		scanf("%d",&A[i]);	
	}	
	
	for(i=0;i<11;i++)
	{
		printf("%d", i);
		for(j=0;j<N;j++)
		{
			if(i==A[j])
			{
				printf(" *");
			}
			
		}		
		printf("\n");

	}

}
	
