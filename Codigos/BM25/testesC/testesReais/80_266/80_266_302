#include <stdio.h>
#include <stdlib.h>
#include <string.h>

main() 
{
	char string[100],inversa[100];
	int i,j;

	gets(string);
	for(i=0;string[i];i++);
	     i = i-1;

	for(j=0;i>=0;j++)
	{
		inversa[j]=string[i];
		i--;
	}
	inversa[j]='\0';

	puts(inversa);
}
