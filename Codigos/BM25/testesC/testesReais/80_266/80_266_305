#include<stdio.h>
#include<string.h>
//#include<conio.h>
int main(){
    char str[200];
    int i,n;
    scanf("%[^\n]",str);
    n = strlen(str);
    for(i=n-1;i>=0;i--){
        printf("%c",str[i]);                    
    }
    //getche();   
}
 
