#include<stdio.h>
#include<string.h>
int main(){
	char str[200],c[5];
	int i,j,cont=0,n;
	scanf("%[^\n]",str);
	scanf("%s",c);
	n = strlen(str);
	for(i=0;i<n;i++){
		if(str[i]==c[0]){
			cont+=1;		
		}
	  
	}
	printf("%d",cont);
}
