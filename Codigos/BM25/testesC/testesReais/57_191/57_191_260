#include <stdio.h>

int main(void){
    int n,contador=0,i;

    scanf("%d",&n);

    while(contador!=2){
        n=n+1;
        contador=0;
        for (i=1; i<=n; i++){
        if ((n%i)==0) contador+=1;
        }
    }
    printf("%d",n);

}
