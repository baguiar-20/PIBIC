#include<stdio.h>
void main()
{
    int vetor[1000];
    int num,i,n,flag;

    flag = 0;

    scanf("%d",&n);

    for(i=0;i<n;i++)
    {
        scanf("%d",&vetor[i]);
    }

    scanf("%d",&num);

    for(i=0;i<n;i++)
    {
        if(vetor[i]==num)
        {
            flag = 1;
            break;
        }
    }

    if(flag==0)
    {
        printf("NAO");
    }
    else
    {
        printf("SIM");
    }

}
