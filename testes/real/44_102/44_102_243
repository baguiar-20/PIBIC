#include <stdio.h>
#include <stdlib.h>

int main()
{
	int n, fat, i;
	
	fat = 1;
	
	scanf("%d", &n); 
	
	
	if (n == 0 || n == 1)
	   printf("%d\n", fat);
	else
	{
	   if (n > 1)
	   {
		   for (i = 2; i <= n; i++)
		      fat = fat * i;
		   printf("%d\n", fat);
	   }
	 }
			     
	
	return 0;
}

