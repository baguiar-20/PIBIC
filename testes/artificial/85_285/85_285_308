#include<stdio.h>
#include<math.h>

int main(){

	int i, num, ordem,cont;	
	
 	 scanf("%d", &ordem);
	
	 cont=0;

	while(ordem>0 && ordem<1000){

	 int vet[ordem];

	 for(i=0; i<ordem; i++){
	
	 	scanf("%d", &vet[i]);
	
	  }

	 scanf("%d", &num);	
	
	 for(i=0; i<ordem; i++){
	 
	   if(num != vet[i]){
	
	      cont++;
	    }  
	 }
	
	if(cont==10){
	 
	  printf("NAO \n");
        }else
	{
	  printf("SIM \n");
	}

	cont=0;
	
	scanf("%d", &ordem);
      }
}	
