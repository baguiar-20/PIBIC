#include <stdio.h>
int main(){

	int vet[15],x,i,cont=0;
	
	for(i=0;i<15;i++)
		scanf("%d", &vet[i]);

	scanf("%d", &x);

	for(i=0;i<15;i++){
		if(vet[i]==x)
			printf("%d\n", (i=i+1));
			cont++;
	}
	if(cont<1)
		printf("NAO");
}		
	
