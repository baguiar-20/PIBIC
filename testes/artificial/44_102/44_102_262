#include <stdio.h>

int main(int argc, char **argv)
{
	int n, i, armazenador;
	armazenador=1;
	scanf ("%d", &n);
	if (n==0) {
		printf ("0");
	} else if (n==1) {
		printf ("1");
	} else if (n>1) {
		for (i=n;i>0;i--) {
			armazenador*=i;
		}
	}
	printf ("%d", armazenador);
			 
	return 0;
}

