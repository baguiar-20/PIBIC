#include <stdio.h> 

int main(){
  int tam, i=0, n;
  int vetor[100];
  
  printf ("Numeros: ");
  
  while (scanf("%d", &n) == 1) {
  vetor[i] = n;
  i++;
  }
  getchar();
  i--;
  printf("Inverso: ");
  
  while(0<=i){
    printf("%d ",vetor[i]);
    i--;
  }
  printf("\n");
  
  return 0;
}