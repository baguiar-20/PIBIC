#include <stdio.h>
#include <string.h>

int main(){

	char str[100];
	int i = 0;

	for(;i<99;i++) str[i] = ' ';
	printf("Nome: ");
	fgets(str,100,stdin);

	printf("Inverso: ");
	for(i = strlen(str)-2; i>=0; i--)printf("%c",str[i]);
	printf("\n");
}
