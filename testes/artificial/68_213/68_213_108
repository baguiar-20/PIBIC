#include<stdio.h>
int main()
{
 int n, mro, mra, i, au;
 scanf("%i", &n);
 int vn[n];

 for(i=0; i<n; i++)
 {
   scanf("%d", &vn[i]);
      mro=vn[0];
      mra=vn[0];
    if(vn[i]>mra)
      mra=vn[i];
    if(vn[i]<mro)
      mro=vn[i]; 
 }
 
  printf("%i\n%i", mro, mra);
}
