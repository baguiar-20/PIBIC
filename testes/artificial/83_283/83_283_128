#include<stdio.h>

int main(){

	int n, i, numero2,j;

	scanf("%d", &n);
	
	if(n==0){
		return 0;
	}	
	
	int vetor[n];

	for(i=0;i<n;i++){

		scanf("%d", &vetor[i]);
	}

	scanf("%d", &numero2);

	for(j=0; j<n+1; j++){

		if(numero2==vetor[j]){
			printf("SIM\n");
		}
		else{
			if(j==n+1){
				printf("NÃO\n");
			}
		}
	}

	while(n!=0){
		scanf("%d", &n);

		int vetor[n];

	for(i=0;i<n;i++){

		scanf("%d", &vetor[i]);
	}

	scanf("%d", &numero2);

	for(j=0; j<n+1; j++){

		if(numero2==vetor[j]){
			printf("SIM\n");
		}
		else{
			if(j==n+1){
				printf("NÃO\n");
			}
		}
	}
	}
	
}